[features]
resolution = true
skip-lint = false

[programs.localnet]
clench = "YS3nPRaMBL4unfow4iqs8rwrXnLLWx2MYucp9ba582m"

[registry]
url = "https://api.apr.dev"

[provider]
cluster = "localnet"
wallet = "~/.config/solana/id.json"

[scripts]
test = "mkdir -p target/idl target/types && cp idl/clench.json target/idl/clench.json && npx ts-mocha -p ./tsconfig.json -t 1000000 tests/**/*.ts"
